<!DOCTYPE html>
<html>
  <head>
    <title>La Fiesta Express</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <!-- Add Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Add Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <style>
      .bg-mexican {
        background: linear-gradient(135deg, #ff6b35 0%, #f7931e 100%);
      }
      .text-mexican {
        color: #ff6b35;
      }
      .hover-mexican:hover {
        background-color: #ff6b35;
        color: white;
      }
    </style>
  </head>

  <body class="bg-gray-50">
    <!-- Navigation Header -->
    <nav class="bg-white shadow-lg sticky top-0 z-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <!-- Logo -->
          <div class="flex items-center">
  <%= link_to root_path, class: "flex items-center" do %>
    <%= image_tag "xpresslogo.jpeg", alt: "La Fiesta Express Logo", class: "h-10 w-auto mr-2" %>
  <% end %>
</div>

          <!-- Navigation Links -->
          <div class="hidden md:flex items-center space-x-6">
            <%= link_to "Menu", categories_path, class: "text-gray-700 hover:text-mexican transition-colors" %>
            <%= link_to "About", restaurant_info_path, class: "text-gray-700 hover:text-mexican transition-colors" %>
            
            <% if user_signed_in? %>
              <%= link_to orders_path, class: "relative text-gray-700 hover:text-mexican transition-colors" do %>
                <i class="fas fa-shopping-cart"></i>
                <% if session[:order_id] %>
                  <span class="absolute -top-2 -right-2 bg-mexican text-white text-xs rounded-full h-5 w-5 flex items-center justify-center">
                    <%= current_user.orders.find_by(id: session[:order_id])&.order_items&.count || 0 %>
                  </span>
                <% end %>
              <% end %>
              
              <div class="relative group">
                <button class="text-gray-700 hover:text-mexican transition-colors">
                  <%= current_user.first_name %> <i class="fas fa-chevron-down ml-1"></i>
                </button>
                <div class="absolute right-0 mt-2 w-48 bg-white rounded-md shadow-lg opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200">
                  <%= link_to "My Orders", orders_path, class: "block px-4 py-2 text-gray-700 hover:bg-gray-100" %>
                  <% if current_user.admin? %>
                    <%= link_to "Admin Panel", categories_path, class: "block px-4 py-2 text-gray-700 hover:bg-gray-100" %>
                  <% end %>
                  <%= link_to "Logout", logout_path, method: :delete, class: "block px-4 py-2 text-gray-700 hover:bg-gray-100" %>
                </div>
              </div>
            <% else %>
              <%= link_to "Login", login_path, class: "bg-mexican text-white px-4 py-2 rounded-lg hover:bg-orange-600 transition-colors" %>
            <% end %>
          </div>

          <!-- Mobile menu button -->
          <div class="md:hidden">
            <button class="text-gray-700 hover:text-mexican">
              <i class="fas fa-bars"></i>
            </button>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mx-4 mt-4" role="alert">
        <%= notice %>
      </div>
    <% end %>
    
    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mx-4 mt-4" role="alert">
        <%= alert %>
      </div>
    <% end %>

    <!-- Main Content -->
    <main>
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white mt-16">
  <div class="max-w-7xl mx-auto px-4 py-10 space-y-8">
    
    <!-- Brand / Description -->
    <div>
      <h3 class="text-2xl font-semibold mb-2">La Fiesta Express</h3>
      <p class="text-gray-300">Authentic Mexican cuisine made with love and tradition.</p>
    </div>

    <!-- Info Grid -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 text-center text-gray-300">
      
      <!-- Location -->
      <div class="flex flex-col items-center">
        <i class="fas fa-map-marker-alt text-red-500 text-3xl mb-2"></i>
        <p class="font-semibold text-white">Location</p>
        <p class="text-gray-400">
          <% if @restaurant_info %>
            <%= @restaurant_info.street %>, <%= @restaurant_info.city %>, <%= @restaurant_info.state %> <%= @restaurant_info.zip_code %>
          <% else %>
            1133 West Main Street, Troy, OH
          <% end %>
        </p>
      </div>

      <!-- Hours -->
      <div class="flex flex-col items-center">
        <i class="fas fa-clock text-red-500 text-3xl mb-2"></i>
        <p class="font-semibold text-white">Hours Today</p>
        <p class="text-gray-400">
          <% if @restaurant_info %>
            <% 
              today = Date.current.strftime("%A").downcase
              hours_method = "#{today}_hours"
              todays_hours = @restaurant_info.respond_to?(hours_method) ? @restaurant_info.send(hours_method) : nil
            %>
            <%= todays_hours || "Call for hours" %>
          <% else %>
            11:00 AM – 9:30 PM
          <% end %>
        </p>
      </div>

      <!-- Phone -->
      <div class="flex flex-col items-center">
        <i class="fas fa-phone text-red-500 text-3xl mb-2"></i>
        <p class="font-semibold text-white">Call Us</p>
        <p class="text-gray-400"><%= @restaurant_info&.phone || "937-703-1371" %></p>
      </div>

    </div>

  </div>
</footer>

  </body>
</html>